<%= form_for @review do |f| %>

<p>
	<%= f.label :rating %><br>
	<%= f.select :rating, [1, 2, 3, 4, 5] %>
</p>

<p>
	<%= f.label :feedback %><br>
	<%= f.text_area :body %>
</p>

<p>
	<%= f.submit "Submit Feedback" %>
</p>
<% end %>
